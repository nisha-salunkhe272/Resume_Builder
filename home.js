// next page :]
// Get the "Build Now" button by its id
const buildNowBtn = document.getElementById('build-now');

// Add a click event listener to the button
buildNowBtn.addEventListener('click', function() {
    // Redirect the user to the next page after clicking the button
    window.location.href = 'index.html';
});